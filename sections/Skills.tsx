"use client";

/**
 * Skills — jangkar scroll-spy. Label section dihapus; nama section
 * kini tampil sebagai judul display raksasa di kartu (ContentCard).
 */
export default function Skills() {
  return (
    <section id="skills" className="flex h-screen w-full items-center justify-end px-8 sm:px-16" />
  );
}
