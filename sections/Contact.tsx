"use client";

/**
 * Contact — jangkar scroll-spy. Label section dihapus; nama section
 * kini tampil sebagai judul display raksasa di kartu (ContentCard).
 */
export default function Contact() {
  return (
    <section id="contact" className="flex h-screen w-full flex-col items-center justify-center px-8">
      <p className="mt-3 text-sm text-muted/70">email@you.dev</p>
    </section>
  );
}
