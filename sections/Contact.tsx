"use client";

/**
 * Contact — jangkar scroll-spy. Label section dihapus; nama section
 * kini tampil sebagai judul display raksasa di kartu (ContentCard).
 */
export default function Contact() {
  return (
    <section id="contact" className="flex h-screen w-full items-center justify-center px-8" />
  );
}
