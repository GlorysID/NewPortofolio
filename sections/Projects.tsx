"use client";

/**
 * Projects — jangkar scroll-spy. Label section dihapus; nama section
 * kini tampil sebagai judul display raksasa di kartu (ContentCard).
 */
export default function Projects() {
  return (
    <section id="projects" className="flex h-screen w-full items-center justify-end px-8 sm:px-16" />
  );
}
